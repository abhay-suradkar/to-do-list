import { StyleSheet } from 'react-native';

export default Inline = StyleSheet.create({
  textBox: {
    color: 'black',
    backgroundColor: 'white',
    fontSize: 30,
    height: 100,
    marginTop: 10,
    padding: 10,
    textAlign: 'center',
    textAlignVertical: 'center',
    borderRadius: 20,
    borderColor: 'red',
    borderWidth: 5,
  },
});
